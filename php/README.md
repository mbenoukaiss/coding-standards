# PHP coding standards

A composer package that can be installed to provide a standard PHP linter and formatter condiguration available
through the `vendor/bin/coding-standards` script.

## Development

If you need to manipulate the composer.json file, you can run the following command:
```shell
docker run --rm --volume .:/app --workdir /app composer <your-command>
```
